<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
</head>

<body>
  <main class="w-full min-h-screen bg-zinc-100 font-['helvetica']">
    <!-- Navbar -->
    <nav class="flex px-10 justify-between py-5 bg-white shadow-md">
      <h3 class="text-2xl font-semibold tracking-tight">Khatabook</h3>
      <div class="navlinks flex gap-5 items-center">
        <a href="/home" class="tracking-tight text-gray-700 hover:text-blue-500">Home</a>
        <a href="/create" class="tracking-tight text-gray-700 hover:text-blue-500">Create New Hisaab</a>
      </div>
    </nav>

    <!-- Main Section -->
    <div class="hisaabs px-10">
      <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight">Create New Hisaab</h3>

      <form action="/createhisaab" method="POST" class="space-y-4 w-full max-w-md p-5 rounded-md bg-white shadow-md">
        <!-- Title Input -->
        <input 
          type="text" 
          name="title"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400"
          placeholder="Shopping ka hisaab, Ghar ka kharch..." 
          required
        >

        <!-- Description Textarea -->
        <textarea 
          name="content"
          rows="5"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
          placeholder="Write Your Hisaab"
          required
        ></textarea>

        <!-- Submit Button -->
        <input 
          type="submit" 
          value="Create Hisaab" 
          class="w-full cursor-pointer px-5 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600 transition"
        >
      </form>
    </div>
  </main>
</body>
</html>
